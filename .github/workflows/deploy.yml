name: Backend CI/CD Pipeline

on:
  push:
    branches:
      - 'feature/user-update'
    paths:
      - '@backend/**'
      - '.github/workflows/deploy.yml'
      - 'terraform/packer/**'
  pull_request:
    branches:
      - 'feature/user-update'
    paths:
      - '@backend/**'
      - '.github/workflows/deploy.yml'
      - 'terraform/packer/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./@backend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew clean build -x test

      - name: Rename JAR file
        run: mv build/libs/*SNAPSHOT.jar build/libs/app.jar

      - name: Upload jar artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: ./@backend/build/libs/app.jar

  build-ami:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download jar artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: artifacts

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Get Subnet ID from Terraform
        working-directory: terraform
        run: |
          terraform init
          echo "SUBNET_ID=$(terraform output -raw public_subnet_a_id)" >> $GITHUB_ENV

      - name: Setup Packer
        uses: hashicorp/setup-packer@main

      - name: Packer Init
        working-directory: terraform/packer
        run: packer init .

      - name: Build AMI with Packer
        working-directory: terraform/packer
        run: |
          packer build \
            -var "app_artifact_path=../../artifacts/app.jar" \
            -var "subnet_id=${{ env.SUBNET_ID }}" \
            .